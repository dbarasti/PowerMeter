# Configurazione Docker per PowerMeter
# Rinomina questo file in .env prima di usare Docker

# Porta Modbus (su Windows potrebbe essere COM3, COM4, etc.)
# In Docker su Windows, potrebbe essere necessario mappare la porta seriale
# Esempio: COM3 -> potrebbe essere /dev/ttyS2 o /dev/ttyUSB0
MODBUS_PORT=/dev/ttyUSB0

# Secret key per JWT (CAMBIARE IN PRODUZIONE!)
SECRET_KEY=change-this-secret-key-in-production-use-a-strong-random-key

# Fuso orario
TIMEZONE=Europe/Rome

# Opzionale: configurazione database (default: SQLite in ./data)
# DATABASE_URL=sqlite:///./data/thermal_tests.db
